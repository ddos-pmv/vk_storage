#include <iostream>
#include <boost/program_options.hpp>

#include "node.h"

int main() {


    // std::vector<std::tuple<std::string, std::string, uint32_t>> initial_data =
    // {
    //     {"key1", "value1", 0}, {"key2", "value2", 10}, {"key3", "value3", 5}};
    // vk::KVStorage<> node;
    // node.store(std::span(initial_data));

    return 0;
}
